declare global {
    interface Window {
        /**
         * The deployment client code -> UW, AC, DL, 3V, 5X, 8X, NZ, MH, KH
         */
      tenant: string;
    }
  }
  
  export {};
  